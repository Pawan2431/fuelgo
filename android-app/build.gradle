// Top-level build file for Android Studio Project
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.1.1'
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
